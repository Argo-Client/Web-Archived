import Head from "next/head";

const Page = (props) => (
  <div>
    <Head>
      <link rel="shortcut icon" href="/favicon.ico" />
      <title>Argo voor Magister 6 </title>
    </Head>
  </div>
);

export default Page;
